<h1>My tasks</h1>
 <% @tasks.each do |task| %>
  <p>
  <input type="checkbox" name="task[title]" <% if task.completed == true %> checked="checked" <% end %>/>
  <%= link_to task.title, task_path(task) %>
  </p>
 <% end %>

